#! /bin/bash
#
# (C) Copyright IBM Corp. 2001, 2003, 2003
#
# $Id$
#
# Invoke the rvm script using environmental variables
#
# @author Jeffrey Palm
# @date   25 Jun 2002
# @modified Steven Augart (ported to bash)
# @date May 31, 2003


# Where are we?
mydir="${0%/*}"

## Load aux. functions
. $mydir/sanity-envars.bash

# Sanity checks for $HOME and $RVM_ROOT.  (They may not be
# set if we invoke this script from someplace other than a shell.)

checkenv HOME "/home/${USER-${LOGNAME-'me'}}"
# Place where source files reside.
checkenv RVM_ROOT "$HOME/rvmRoot"
# Place where RVM bootimage, booter, and runtime support files will be placed.
checkenv RVM_BUILD "$HOME/rvmBuild"

# Pass these along as parameters to runrvm.
. "${mydir}/runrvm" "$HOME" "$RVM_ROOT" "$RVM_BUILD" "$@"
